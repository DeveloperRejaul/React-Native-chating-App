import {StyleSheet, View} from 'react-native';
import React from 'react';
import ChatUI from './ChatUI';

export default function ChatUiIndex({route}) {
  return <ChatUI route={route} />;
}

const styles = StyleSheet.create({});
